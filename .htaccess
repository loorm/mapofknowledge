Options -Indexes
RewriteEngine On

# Proxy all requests to Node.js Express via Unix domain socket.
# Unix sockets bypass TCP firewalling on this host.
RewriteCond %{REQUEST_URI} !^/\.well-known
RewriteRule ^(.*)$ unix:/data01/virt147958/mok.sock|http://mok/$1 [P,L]
